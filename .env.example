DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Local dev (without Docker)
# Leave empty to use SQLite.

# Docker Compose sets this automatically:
# DATABASE_URL=postgres://pick_a_print:pick_a_print@db:5432/pick_a_print

# Photogrammetry pipeline (same docker-compose.yml)
# PIPELINE_DATA_DIR=./data/jobs
# SCAN_CELERY_BROKER_URL=redis://localhost:6379/1
# SCAN_MAX_UPLOAD_MB=200
# MINIO_ENDPOINT=http://localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=scans

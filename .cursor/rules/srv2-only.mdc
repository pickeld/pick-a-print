---
description: Edit and deploy only on srv2
globs: **/*
alwaysApply: true
---

This project runs on **srv2** (`192.168.127.252`). The canonical workspace is `~/pick-a-print` on that host.

- Do **not** edit files under `/Users/pickeld/Desktop/pick-a-print` or any Mac-local clone (read-only for sync).
- SSH: `pickeld@192.168.127.252`, workspace `~/pick-a-print`.
- Deploy with: `cd ~/pick-a-print && docker compose up -d --build`
